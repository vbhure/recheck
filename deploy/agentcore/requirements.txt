# Runtime dependencies for deploy/agentcore/app.py on Bedrock AgentCore Runtime.
# Recheck's own pins (pyproject.toml), plus the AgentCore runtime SDK.
# The recheck package itself is NOT installed from here: cloudshell.sh stages
# src/recheck next to app.py, and the toolkit's Dockerfile copies it in
# (`COPY . .`, then `python -m app` from /app, so `import recheck` resolves).
# strands-agents bundles the Bedrock provider (boto3/botocore); no extra needed.
strands-agents==1.55.1
pydantic==2.13.5
pypdf==6.18.1
bedrock-agentcore==1.23.0
boto3==1.43.93
botocore==1.43.93
